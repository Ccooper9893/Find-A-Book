{
  "name": "findabook-app",
  "version": "1.0.0",
  "description": "Search website to find and save books using Google's book API",
  "main": "server/server.js",
  "engines": {
    "node": "16.18.0"
  },
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"cd server && npm run watch\" \"cd client && npm start\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build"
  },
  "keywords": [],
  "author": "Cody Cooper",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^5.1.0"
  }
}
